name: Daily job

on:
  schedule:
    - cron: "00 0 * * *"

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v2
    - name: Run Python script
      run: python 经典诵读.py
    - name: Run Python script
      run: python 宝宝.py  
